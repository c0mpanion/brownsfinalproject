#include "scoring.h"

#include <stdio.h>

/*
 * Function:  scoring
 * Purpose: Computes a severity score for a given collision
 * Inputs:
 *          kills        -   Number of total deaths in a collision
 *          injuries     -   Number of total people injured from a collision
 * Output:
 *          finalscore   -   Result from algorithm
 */

int severityscore (int kills, int injuries)
{
    int finalscore = 0;
    return finalscore;
}